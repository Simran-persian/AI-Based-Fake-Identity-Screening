{
  "name": "sentry-ai-document-screening",
  "version": "1.0.0",
  "description": "AI-Based Fake Identity & Document Screening System",
  "main": "backend/dist/server.js",
  "scripts": {
    "start": "node backend/dist/server.js",
    "dev": "ts-node backend/src/server.ts",
    "build": "cd backend && npm run build",
    "vercel-build": "cd backend && npx prisma generate && npm run build"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
